# Claude API
ANTHROPIC_API_KEY=sk-ant-your-key-here
CLAUDE_MODEL=claude-opus-4-6

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pixelcoder
DB_USER=postgres
DB_PASSWORD=pixelcoder

# Cycle settings (interval, budget, etc.) live in the database.
# Seeded by migration with defaults. Editable via /admin panel.

# Admin
API_SECRET=change-me-to-something-secret

# Server
PORT=3000
NODE_ENV=development
